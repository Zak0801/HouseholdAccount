<p>Index Page</p>
<% @items.each do |item|%>
    <%= item.using_month%>月
    <%= item.using_day%>日
    <%= item.price%>円
    <%= item.category%>
    [<%= link_to("編集","/item/#{item.id}/edit")%>]
    [<%= link_to("削除","/item/#{item.id}/destroy",{method: "post"})%>]<br>
<% end %>
